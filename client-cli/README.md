# EnergyGrid CLI Tool

Command-line tool to fetch telemetry from 500 solar inverters.

## Usage

```bash
npm install
npm start
```

Results saved to `results.json`

## Configuration

Edit `config/config.js` to change settings.

